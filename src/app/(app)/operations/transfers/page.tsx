import { TransfersClient } from './transfers-client';

export const metadata = { title: 'Transferencias — The Builder\'s House' };

export default function TransfersPage() {
  return <TransfersClient />;
}
