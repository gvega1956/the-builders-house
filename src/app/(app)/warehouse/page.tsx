import { WarehouseClient } from './warehouse-client';

export const metadata = { title: 'Almacenes — The Builder\'s House' };

export default function WarehousePage() {
  return <WarehouseClient />;
}
